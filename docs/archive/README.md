# Archive

Superseded or historical docs kept for context. Nothing here is the active source of truth.

- `todo.md`, `backend.md`, `frontend.md` — original umbrella implementation plans, now broken into the dated plans under `docs/plans/`.
- `spec-backport.md` — one-time edit plan for backporting mockup decisions into `SPEC.md`. Edits are applied.
- `ui-mockup-prompt.md` — original prompt used to generate the standalone shadcn mockup that seeded the frontend.

Active workflow lives in:
- `docs/SPEC.md` — product spec
- `docs/FUTURE.md` — scratchpad for ideas not yet specced
- `docs/specs/`, `docs/plans/`, `docs/prompts/` — dated pipeline artifacts (`YYYY-MM-DD-NN-slug.md`)
- `docs/handoff/` — implementer handoff notes per step
